using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class FailController : MonoBehaviour {




	// Use this for initialization
	void Start () {
		
	}

    void OnTriggerEnter2D (Collider2D other) //funkcja zwróci nam jaki obiekt skolidowa³ z obiektem, który bêdzie mia³ przypiêty ten skrypt
    {
        if(other.gameObject.name == "Dron") // jeœli koliduje z dronem to...
        {
            other.gameObject.GetComponent<Animator>().SetTrigger("fail"); //zmieñ jego pozycje na startPoint
        }
    }
	
}
